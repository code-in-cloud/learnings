#include "search.h"

struct Search_Result
{
    int index;
    bool found;
};

result_t* binary_search(int* list, int size, int key)
{
    result_t *result = NULL;
    if(list && size > 0)
    {
        
    }

    return result;
}